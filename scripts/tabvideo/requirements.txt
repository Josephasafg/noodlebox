# The tab video reader. Not part of the app bundle: it runs as a local service
# beside the dev server, because reading notation off a video needs a video
# decoder and OpenCV and a browser has neither.
opencv-python>=4.9
numpy>=1.26

# The service the app talks to.
fastapi>=0.115
uvicorn>=0.30
yt-dlp>=2024.8.6

# Tests only.
pytest>=8.0
httpx>=0.27
